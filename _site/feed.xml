<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://www.geemichael.com/feed.xml" rel="self" type="application/atom+xml" /><link href="https://www.geemichael.com/" rel="alternate" type="text/html" /><updated>2026-01-31T23:53:11-08:00</updated><id>https://www.geemichael.com/feed.xml</id><title type="html">Michael Ge | Software Engineer &amp;amp; Graphic Artist</title><subtitle>Portfolio of Michael Ge featuring software engineering projects</subtitle><author><name>Michael Ge</name></author><entry><title type="html">Toward the Light: Behind the Scenes</title><link href="https://www.geemichael.com/graphics/2018/08/22/toward-the-light-bts.html" rel="alternate" type="text/html" title="Toward the Light: Behind the Scenes" /><published>2018-08-22T00:00:00-07:00</published><updated>2018-08-22T00:00:00-07:00</updated><id>https://www.geemichael.com/graphics/2018/08/22/toward-the-light-bts</id><content type="html" xml:base="https://www.geemichael.com/graphics/2018/08/22/toward-the-light-bts.html"><![CDATA[This long-overdue post is a visual effects breakdown for some of my favorite shots in my VES 153AR: Intermediate Animation final project and first complete short animation, <i>Toward the Light</i>. To follow along, you can find the film <a class="link" href="https://www.youtube.com/watch?v=Np4Kl2DHNDQ">here</a>. I'll be referencing times based on the YouTube link to discuss the animation.

# 0:05 &ndash; Complex Opening Scene

One of the prettiest scenes, and also one of the most computationally expensive, is the opening sequence. Being the first scene of my first animation, I definitely set a stylistic expectation that would quickly devolve as the animation goes on. The scene is composed of several layers composited together: the fire and foreground, the trees and shrubbery, the flowers, and the mountains/northern lights sky. Initial attempts at the scene were ambitious, to say the least: polygonal trees and globally illuminated flowers and fire were the most expensive culprits of increased render time.

To fix the trees, I opted for two orthogonally intersecting planes with several transparent tree textures on each plane. The effect provides a huge save on the number of polygons, but it can look a bit tacky. If you look carefully at some of the trees, you can even see symmetry across their centers. 

The fire is a particle system with additional compositing in after effects. In Cinema 4D, I created a particle sytsem that emitted planes with a fire texture in random orientations and scales. To get the illumination on the ground, GI took too long to render, so I instead set up a light source within the fire with photometric intensity. This tended to render much faster than GI and had a great, if not identical effect. After touching it up with some Trapcode Shine, the final effect was a pretty convincing fire.

# 0:19 &ndash; Particle Fireflies

At this point in the animation, I had already begun to feel the toll of long render times. Switching particle systems from Cinema 4D to Adobe After Effects greatly increased the flexibility of the swarming sensation I tried to produce with the fireflies. From here on out, most of the assets are rendered on a transparent background and composited together.

# 0:34 &ndash; Aurora Borealis

The northern lights effect was based on long planar strips, bent in a wavy fashion. The texture had luminance and glow with alpha transparency along the edges of the plane. There were some minor artifacts along the edges of the final render while compositing the northern lights onto the background which could have been avoided with a certain setting, though I can't remember what that setting is.

# 0:47 &ndash; The Moth

The moth was my first-ever character rig. The head, eyes, eyebrows, body, and wings have full programmatic control via XPresso, allowing me to pose the moth in a variety of configurations. The most interesting part of the rig is the legs. The six legs follow inverse kinematics with Cinema 4D's bone tools, but to save myself the animation time of keyframing each leg movement, I devised a programmatic solution. Attached to the body are six circles representing the ranges each of the six feet can reside in. As the body moves forward, the ranges move along with it, though the feet stay in place. At some point, the body moves far enough such that the feet are out of the range of its respective circle. When this happens, I programmed the feet to move back to the center of the circular range. The result is a jittery, insect-like walk that still has the liveliness of IK-spline legs.

# 1:37 &ndash; Moth Closeup

Just wanted to touch on the movement of this moth. Toward the end of the animation process, I began taking note of earlier animation mistakes: much of the moth's movement before this point was robotic and linear. I found that an easy fix is staggering the keyframes of different motions so that movements overlapped with one another. This greatly improved the realism of the moth for the rest of the film.

# 2:17 &ndash; Ending

The fire trails behind the moth were all done in After Effects using Trapcode Particular and Trapcode Shine. This just goes to show that Cinema 4D is not primarily a visual effects tool, but rather a motion graphics tool, and many of the effects that really make an animation shine can be emulated in After Effects! Finding the right balance between animation and post-production can significantly increase your productivity and output.]]></content><author><name>Michael Ge</name></author><category term="graphics" /><category term="cinema 4d r15" /><category term="3d animation" /><category term="toward the light" /><category term="particles" /><category term="rigging" /><summary type="html"><![CDATA[This long-overdue post is a visual effects breakdown for some of my favorite shots in my VES 153AR: Intermediate Animation final project and first complete short animation, Toward the Light. To follow along, you can find the film here. I’ll be referencing times based on the YouTube link to discuss the animation.]]></summary></entry><entry><title type="html">Arch Linux: Chromebook C720 Webcam Microphone Disappeared</title><link href="https://www.geemichael.com/technology/2018/07/01/microphone-webcam-detection.html" rel="alternate" type="text/html" title="Arch Linux: Chromebook C720 Webcam Microphone Disappeared" /><published>2018-07-01T00:00:00-07:00</published><updated>2018-07-01T00:00:00-07:00</updated><id>https://www.geemichael.com/technology/2018/07/01/microphone-webcam-detection</id><content type="html" xml:base="https://www.geemichael.com/technology/2018/07/01/microphone-webcam-detection.html"><![CDATA[After plugging in a third-party microphone, my laptop stopped detecting my webcam and microphone in its listed devices.

Chromium stopped recognizing my webcam and microphone. In pavucontrol, the input audio devices did not include my microphone. Checking journalctl, I grep'ed for Camera logs and found the following issue:

```
kernel: uvcvideo 1-3:1.0: Entity type for entity Camera 1 was not initialized!
```

After looking around, I was told to install '''v4l-utils'''. After doing so, I checked whether the internal webcam existed with --list-devices. Sure enough, it was there.

The microphone was still broken. In ```/etc/pulse/default.pa```, I uncommented the following:

```
 load-module module-alsa-source device=hw:0,0
  # the line above should be somewhere before the line below
 .ifexists module-udev-detect.so
 ```

Then, I restarted pulseaudio to apply the new settings:
```
$ pulseaudio -k ; pulseaudio -D
```

Then things we working again!]]></content><author><name>Michael Ge</name></author><category term="technology" /><category term="arch linux" /><category term="webcam" /><category term="microphone" /><summary type="html"><![CDATA[After plugging in a third-party microphone, my laptop stopped detecting my webcam and microphone in its listed devices.]]></summary></entry><entry><title type="html">SSH: How to Set Up a Simple Local Media Server</title><link href="https://www.geemichael.com/technology/2018/06/21/local-media-server.html" rel="alternate" type="text/html" title="SSH: How to Set Up a Simple Local Media Server" /><published>2018-06-21T00:00:00-07:00</published><updated>2018-06-21T00:00:00-07:00</updated><id>https://www.geemichael.com/technology/2018/06/21/local-media-server</id><content type="html" xml:base="https://www.geemichael.com/technology/2018/06/21/local-media-server.html"><![CDATA[While getting ready for my big move to Facebook, I came across way too many old laptops. One in particular had a solid 128GB of disk space that I couldn't bring myself to throw into the electronics recycling bin. After looking around online for some good potential use cases, I finally decided that I'd try my hand at setting up a simple Linux-based local media server.

For this project, I wanted to run my server constantly in my room, allowing me to upload photos, videos, and documents from any device on the network to the server safely and securely. It turns out, the process is actually quite simple, consisting of the following steps:
1. Create an <a class="link" href="https://www.geemichael.com/technology/2018/01/17/arch-usb/">Arch Linux installation medium</a>
2. <a class="link" href="https://www.geemichael.com/technology/2017/12/18/arch-install-chromebook/">Install Arch on your server laptop</a>
3. Start an SSH server
4. (Optional) Install a terminal on your phone
5. Upload from any device to the server

# The Server Part

The following section details how to set up the "server" part of the media server. Before beginning, be sure to have a user account named ```media```.

## Starting an SSH Server

This section will describe how to start an SSH server on your server machine.

First, install `openssh`:
```console
sudo pacman -S openssh
```

Then start the sshd service:
```console
# systemctl start sshd.service
```

your SSH server should now be started. You should be able to run the following from your server and successfully SSH into the server itself:

```console
media@myhostname ~]$ ssh media@localhost
```

## Connecting to the SSH server

Let's now confirm that we can successfully SSH into the server. Find your server's local IP address:
```console
ip addr | grep "inet"
```
This number will have the form X.X.X.X

Then, from a different device on the same network, you should be able to SSH into the server:

```console
ssh media@X.X.X.X
```

## Uploading Files

If the above works, you should now be able to upload files to the media server using `scp`. To upload a folder, use the `-r` flag. Otherwise, order the arguments to be source file to destination:

```console
scp -r ./local_dir media@X.X.X.X:/path/to/destination
```

## Termux

You can even do the same process from your Android phone. Termux is a terminal app designed for mobile phones. Simply install Termux, download the `openssh` package, and perform a secure copy of your mobile device's files. To get access to Android's photos, you may need to run the following in Termux:
```console
termux-setup-storage
```
This gives you permissions to the media folders on your device and populates the home folder with a storage subdirectory.

# The Media Part

Here is how to turn your server machine into a minimalist media-viewing device using the Chromium web browser.

We'll give our  ```media``` user media-viewing capabilities by running Chromium full-screen when ```startx``` is called. Within the browser, you should be able to open PDFs, movie files, and images, making it a great all-in-one media-viewing application.

First, run the following installation:
```console
sudo pacman -S xorg xorg-xinit chromium
```

After the install is complete, edit your ```.xinitrc``` file to include the ```chromium``` command. This file is executed when ```startx``` is run:

```console
echo chromium >> ~/.xinitrc
```

Personally, I had some issues with the display not fully filling the screen when I ran ```startx```. To fix this, I changed the Chromium config file at ```~/.config/chromium/Default/Preferences``` to have the ```window_placement``` object fill the entire screen's coordinates. See <a class="link" href="https://unix.stackexchange.com/questions/273989/how-can-i-make-chromium-start-full-screen-under-x">this</a> stackoverflow link for more information.

And with that, you should be able to reuse any old laptops for file storage and file previewing!]]></content><author><name>Michael Ge</name></author><category term="technology" /><category term="arch linux" /><category term="ssh" /><category term="networks" /><category term="termux" /><summary type="html"><![CDATA[While getting ready for my big move to Facebook, I came across way too many old laptops. One in particular had a solid 128GB of disk space that I couldn’t bring myself to throw into the electronics recycling bin. After looking around online for some good potential use cases, I finally decided that I’d try my hand at setting up a simple Linux-based local media server.]]></summary></entry><entry><title type="html">Pacman: File Conflicts</title><link href="https://www.geemichael.com/technology/2018/02/28/pacman-file-conflicts.html" rel="alternate" type="text/html" title="Pacman: File Conflicts" /><published>2018-02-28T00:00:00-08:00</published><updated>2018-02-28T00:00:00-08:00</updated><id>https://www.geemichael.com/technology/2018/02/28/pacman-file-conflicts</id><content type="html" xml:base="https://www.geemichael.com/technology/2018/02/28/pacman-file-conflicts.html"><![CDATA[I got a Jupyter error: failed to commit transaction (conflicting files) while trying to update my packages.

Not sure how I managed to get my packages out of sync. Running pacman -Qo on the conflicting files specified that pacman wasn't managing the files that had the conflicts, and I definitely don't do any manual installs.

I ran pacman -Syu --force to get it to install. That's probably not the best way to handle this problem, like at all, but if it's for something like Jupyter Notebook, it's fine.]]></content><author><name>Michael Ge</name></author><category term="technology" /><category term="arch linux" /><category term="packages" /><summary type="html"><![CDATA[I got a Jupyter error: failed to commit transaction (conflicting files) while trying to update my packages.]]></summary></entry><entry><title type="html">Making an Arch Linux USB Flash Install Medium</title><link href="https://www.geemichael.com/technology/2018/01/17/arch-usb.html" rel="alternate" type="text/html" title="Making an Arch Linux USB Flash Install Medium" /><published>2018-01-17T00:00:00-08:00</published><updated>2018-01-17T00:00:00-08:00</updated><id>https://www.geemichael.com/technology/2018/01/17/arch-usb</id><content type="html" xml:base="https://www.geemichael.com/technology/2018/01/17/arch-usb.html"><![CDATA[I managed to mess up my USB installation medium tonight... managed to turn it into a NTFS, read-only USB, so I couldn't delete anything.

Since I just wanted to delete everything, I just wiped the drive using `parted`, downloaded the iso, and copied it using `dd`.

# Parted Wipe
```
parted \dev\sdb
(parted) mklabel gpt
(parted) mkpart 0% 100%
```

Then format:

```
mkfs.ext4 /dev/sdb1
```
# dd
Then download an iso from one of the online repositories. Suppose we place it in `~/archlinux.iso`. The command to copy the image onto the USB is:

```
sudo dd bs=4M if=~/archlinux.iso of=/dev/sdb status=progress oflag=sync
```

That's it!]]></content><author><name>Michael Ge</name></author><category term="technology" /><category term="arch linux" /><category term="usb" /><summary type="html"><![CDATA[I managed to mess up my USB installation medium tonight… managed to turn it into a NTFS, read-only USB, so I couldn’t delete anything.]]></summary></entry><entry><title type="html">Arch Linux: Post-Install Notes</title><link href="https://www.geemichael.com/technology/2018/01/17/arch-post-install.html" rel="alternate" type="text/html" title="Arch Linux: Post-Install Notes" /><published>2018-01-17T00:00:00-08:00</published><updated>2018-01-17T00:00:00-08:00</updated><id>https://www.geemichael.com/technology/2018/01/17/arch-post-install</id><content type="html" xml:base="https://www.geemichael.com/technology/2018/01/17/arch-post-install.html"><![CDATA[Here's some things you should do after successfully installing Arch Linux.

# Install Useful Packages

- grub: for booting
- iw, wpa_supplicant: wifi stuff
- dialog: to allow wifi-menu
- intel-ucode: for Intel CPUs
- xorg-server: display server
- xorg-xinit: to startx
- xterm: because xinit needs a terminal
- chromium: to browse the Internet
- Emacs: because everyone needs a _good_ text editor
- arandr: screen extend
- ttf-dejavu: fonts
- xbindkeys: shortcuts
- xorg-xmodmap: change what the buttons do
- xorg-xbacklight: backlight
- xf86-input-libinput: mouse
- scrot: print screen
- pulseaudio: audio
- alsa-utils: audio
- pavucontrol: audio UI

# Make a user

```
useradd -m -G wheel -s /bin/bash username
passwd username
nano /etc/sudoers
# Comment out %wheel ALL=(ALL) ALL
```

# WLAN and Bluetooth Improvement
```
/etc/modprobe.d/ath9k.conf

options ath9k bt_ant_diversity=1 ps_enable=0
btcoex_enable=1
```

Setting `ps_enable=1` might freeze your system, but it saves power.

# Touchpad Settings
```
/etc/X11/xorg.conf.d/50-cros-touchpad.conf

Section "InputClass"
    Identifier "touchpad peppy cyapa"
        Driver "synaptics"
        MatchIsTouchpad "on"
        MatchDevicePath "/dev/input/event*"
            Option "TapButton1" "1"
            Option "TapButton2" "3"
            Option "TapButton3" "2"
	    Option "ClickFinger2" "3"
	    Option "ClickFinger3" "2"
            Option "VertTwoFingerScroll" "on"
	    Option "HorizTwoFingerScroll" "on"
            Option "FingerLow" "10"
	    Option "FingerHigh" "10"
EndSection
```

# xbindkeys
```
~/.xbindkeysrc

"amixer -c 1 set Master 5%+"
m:0x0 + c:76

"amixer -c 1 set Master 5%-"
m:0x0 + c:75

"pacmd dump|awk --non-decimal-data '$1~/set-sink-mute/{system ("pacmd "$1"
"$2" "($3=="yes"?"no":"yes"))}' > /dev/null"
m:0x0 + c:74 

"xbacklight -dec 5"
m:0x0 + c:72

"xbacklight -inc 5"
m:0x0 + c:73

"emacsclient -c"
m:0xc + c:26

"scrot -s '%Y-%m-%d-%s.png' -e 'mv $f ~/'"
m:0x4 + c:124

"systemctl suspend"
m:0x8 + c:124
```]]></content><author><name>Michael Ge</name></author><category term="technology" /><category term="arch linux" /><category term="packages" /><summary type="html"><![CDATA[Here’s some things you should do after successfully installing Arch Linux.]]></summary></entry><entry><title type="html">Binary Classification Metrics</title><link href="https://www.geemichael.com/datasci/2018/01/15/classification-metrics.html" rel="alternate" type="text/html" title="Binary Classification Metrics" /><published>2018-01-15T00:00:00-08:00</published><updated>2018-01-15T00:00:00-08:00</updated><id>https://www.geemichael.com/datasci/2018/01/15/classification-metrics</id><content type="html" xml:base="https://www.geemichael.com/datasci/2018/01/15/classification-metrics.html"><![CDATA[Depending on the situation, the simple "number of correct classifications" error metric might not be the best metric to use in binary classification. Here, we explore several metrics and how they might be used for different problems.

# Experimental Design

In binary classification, it is common practice to label the "afflicted" case as a 1. For example, if we try to predict whether an individual has cancer or not, we would label our training data with a 1 if the unit is afflicted with cancer. This isn't too big of a deal as long as you're clear, but the statistics in the following section make more intuitive sense with this convention.

# Binary Classification Statistics
There are $2^2 = 4$ binary classification statistics to consider.

## True Positive (TP):

A *true positive* is a classification where $\hat{y} = 1$ and $y = 1$. That is, the model predicts the positive case (1) correctly.

## False Positive (FP):

A *false positive* is a classification where $\hat{y} = 1$ and $y = 0$. The model makes a positive prediction even though the true class is negative. This is also known as Type-I error.

## False Negative (FN):

A *false negative* occurs when $\hat{y} = 0$ and $y = 1$. Here, the model predicts the negative case, even though the sample is actually a positive case. This is also known as Type-II error.

## True Negative (TN):

A *true negative* occurs when both $\hat{y}$ and $y$ are 0. The model successfully classifies a sample in the negative case.

With these statistics, we can then define detection metrics.

# Detection Metrics:

There are several metrics that can be used to measure the efficacy of your model:

## Accuracy

Accuracy is the most intuitive metric you might use. It's simply the proportion of correct classifications into either class:

$$\textit{Accuracy} = \frac{TP + TN}{TP + TN + FP + FN}$$

This is a reasonable metric to use when both classes are equally important.

## True Positive Rate

The True Positive Rate (TPR), also known as the *sensitivity*, is a measurement of how sensitive our model is to positive cases:

$$TPR = \frac{TP}{TP + FN}$$

If we are predicting whether units have diseases or not, we might want a sensitive model so we do not miss any cases. If the sensitivity is low, the test wouldn't be a strong indicator of the disease.

We would like the FPR to be as low as possible, meanig

## True Negative Rate

The True Negative Rate (TNR), also known as the *specificity*, is a measurement of units correctly classified into the negative case out of all units in the negative case.

$$TNR = \frac{TN}{FP + TN}$$

If our specificity is low, then our model might be incorrectly classifying everyone as the positive case, which would be uninformative.

## F1-Score

We might want to consider both how often we correctly mark something as the positive case in addition to its sensitivity. We can then use a metric based on *precision* and *recall*. Precision is defined as the fraction of correct positive cases:

$$P = \frac{TP}{TP + FP}$$

Recall (R) is the fraction of cases marked as positive, also known as the sensitivity of TPR.

The F1-Score or F-Score is known as the harmonic mean of precision and recall:

$$F_1 = 2\frac{P\cdot R}{P+R}$$

With these metrics, it's up to the data scientist to decide which metrics are the most important to report depending on the experiment being run.]]></content><author><name>Michael Ge</name></author><category term="datasci" /><category term="machine learning" /><category term="accuracy" /><category term="error" /><summary type="html"><![CDATA[Depending on the situation, the simple “number of correct classifications” error metric might not be the best metric to use in binary classification. Here, we explore several metrics and how they might be used for different problems.]]></summary></entry><entry><title type="html">Probabilitistic Classification</title><link href="https://www.geemichael.com/datasci/2018/01/14/probabilistic-classification.html" rel="alternate" type="text/html" title="Probabilitistic Classification" /><published>2018-01-14T00:00:00-08:00</published><updated>2018-01-14T00:00:00-08:00</updated><id>https://www.geemichael.com/datasci/2018/01/14/probabilistic-classification</id><content type="html" xml:base="https://www.geemichael.com/datasci/2018/01/14/probabilistic-classification.html"><![CDATA[In binary classification problems, we have an input feature vector and we'd like to classify it into one of two classes. We did this by minimizing reasonable loss functions based on activation functions. In this very long post, we'll take a probabilistic approach to classification and detail the generative framework.

# Generative Probabilistic View

There are two main probabilistic views of a classification problem: the generative and discriminative views. For some intuition of the generative model, suppose we're playing God and we're trying to construct wolf and sheep from scratch. We might think there's a particular distribution of wolf and sheep in the world that we would like to maintain. In terms of creating the animal, if we know that we're creating a wolf, the probability that the animal has the sharp teeth goes up and the probability that the animal has wool goes down. We might find that wooly wolves are less likely than sharp-toothed wolves, and wooly sheep are more likely than sharp-toothed sheep. Finally, if we know there is only one wolf for every million sheep, we might find that the probability of generating a wolf with any characteristics extremely unlikely. Most importantly, note that we have a model to decide how to generate the animals: specifically the relative proportions I'd like to maintain and what the features of the animal are.

In the *generative* view, we model the problem as trying to calculate the joint probability of both the data and the outcome:

$$p(\mathbf{x}, y) = \overbrace{p(\mathbf{x}|y)}^\text{class-conditional}\underbrace{p(y)}_\text{class}$$

The *class* probability is the probability that a datapoint is in a certain class $y$, while the *class-conditional* probability is the probability that a datapoint has a certain feature vector given the class.

Note, we'll also change our output space slightly to be:

$$\mathcal{y} = \{0, 1\}$$

This will be useful when expressing our class prior.

# Class Prior
In binary classification's $p(y)$, we need to define a prior distribution over the two classes. Let $\theta$ be the number of units in class 1.

$$p(y=1; \theta) = \theta, p(y=0; \theta) = 1-\theta$$

Because of our output space change, we can equivalently write this as:

$$p(y) = \theta^y(1-\theta)^{1-y}$$

We will later find an estimate of $\theta$ using Bayesian methods.

# Class-Conditional Prior

The class-conditional distribution is whatever distribution that defines $\mathbf{x}$. Depending on your feature vector, there are many ways to model $\mathbf{x}$. If they're all Normally distributed, you might consider a multivariate Gaussian to model the class-conditional:

$$p(\mathbf{x}|y=0; \mathbf{\mu}_0, \mathbf{\Sigma}_0)$$

$$p(\mathbf{x}|y=1; \mathbf{\mu}_1, \mathbf{\Sigma}_1)$$

Of course, keep in mind that choosing distributions that yield conjugacy with your class priors or otherwise make computation easier might be favorable, but you might introduce model bias.

We'll begin by assuming each of the covariates in $\mathbf{x}$ come from binary, discrete spaces:

$$p(\mathbf{x}|y; \mathbf{\pi}_0, \mathbf{\pi}_1)$$

We can think of each $\pi$ as being a $ \| \mathbf{x} \| \times 2$ matrix of probabilities, 2 because each of the features will be from a discrete set of binary values. These $\pi$ parameters will be learned from the data.

# Maximum Likelihood Estimation

We've defined a generative model and specified the parameters to learn. Now onto the learning. We calculate the MLE of the parameters as the values that minimize the negative log-likelihood of the data:

$$\min_{\mathbf{\pi}_0, \mathbf{\pi}_1, \theta}\mathcal{L}(\mathbf{\pi}_0, \mathbf{\pi}_1, \theta) = \min_{\mathbf{\pi}_0, \mathbf{\pi}_1, \theta} - \sum_{i=1}^n \left(\ln p(y, \theta) + \ln p(\mathbf{x}_i|y; \mathbf{\pi}_0, \mathbf{\pi}_1)\right)$$

Again, depending on the specified class and class-conditional distributions, there may be conjugate results that would make this easy to calculate, or you might have to use gradient descent to optimize the loss. The minimization would yield the optimal parameters under the MLE setting.

# Sentiment Analysis

Let's look at an example problem. One popular area in classification is *sentiment analysis.* Given a text document $\mathbf{x}$ and a vocabulary $V$, we would like to classify the document as being a positive or negative sentiment. 

Here are two examples:

*I loved that movie! The plot was so wonderful.* has a positive sentiment.

*I hated that movie. There was no plot.* has a negative sentiment.

## Feature Representation: Multinoulli

We can let $y=1$ be the label for a positive sentiment, and $y=0$ be the negative sentiment. Furthermore, our $\mathbf{x}$ text will be transformed into a binary vector with an indicator variable for each word in the dictionary:

$$\phi(\mathbf{x}) = \underbrace{[1, 0, 1, 0, 0, \ldots, 0]}_{\{0, 1\}^{|V|}}$$

A 1 at index $i$ indicates the $i$th word in the dictionary exists in document $\mathbf{x}$. This is known as the multivariate form of the Bernoulli, or the Multinoulli.

## Multinomial Distribution and MLE

We might want to consider the counts of different words rather than their existence alone. Instead of using the Multinoulli distribution which only flags whether a word is seen or not, we can use a generalization of the Binomial (also a generalization of the Multinoulli) called the Multinomial distribution.

$$p(\mathbf{x}; \mathbf{\pi}) \propto \prod_{j=1}^m \pi_j^{x_j}$$

Let's take a slight tangent and calculate the MLE over *a set of feature vectors (without considering classes)*. Using a monotonic log transformation:

$$\begin{align*}
& \arg_{\pi \ge 0}\max \prod_{i=1}^n p(\mathbf{x}_i; \pi) \\
& = \arg_{\pi \ge 0}\max \sum_{i=1}^n \sum_{j=1}^m x_{ij} \ln \pi_j \\
& \text{ s.t. } \sum_{j=1}^m \pi_j = 1\end{align*}$$

We can then optimize this using Lagrange multipliers:

$$L(\mathbf{\pi}, \lambda) = \sum_{i=1}^n \sum_{j=1}^m x_{ij} \ln \pi_j + \lambda \left(1 - \sum_{j=1}^m \pi_j \right)$$

$$\frac{\partial L(\pi, \lambda)}{\partial \pi_j} = \sum_{i=1}^n \frac{x_{ij}}{\pi_j} - \lambda = 0$$

$$\pi_j = \sum_{i=1}^n \frac{x_{ij}}{\lambda}$$

$$\frac{\partial}{\partial \lambda} L(\pi, \lambda) = 1 - \sum_{j=1}^m \pi_j$$

$$\sum_{j=1}^m \pi_j = 1$$

$$\sum_{i=1}^n \sum_{j=1}^m \frac{x_{ij}}{\lambda} = 1$$

$$\lambda = \sum_{i=1}^n \sum_{j=1}^m x_{ij}$$

Finally, we have an expression for $\pi$:

$$\hat{\pi}_{MLE} = \frac{\sum_{i=1}^n \mathbf{x}_i}{\sum_{i=1}^n \sum_{j=1}^m \mathbf{x}_{ij}} = \frac{\mathbf{X}^\top \mathbf{1}}{\mathbf{1X1}}$$

The intuition behind this formula is that the maximum likelihood estimator for $\pi$ is quite simply the proportion of times a word occurs out of all the words.

# Multinomial Binary-Class Naive Bayes

We took some time to understand the form of the parameters $\pi$ when calculating the MLE. Now, we'll see what happens when we incorporate class probabilities. To do this, we'll take the Bayesian approach, Naive Bayes.

Here, we define our generative model as:

$$p(\mathbf{x}, y) = p(y; \theta) p(\mathbf{x} | y; \mathbf{\pi}_0, \mathbf{\pi}_1)$$

Our class probability is distributed Bernoulli:

$$p(y; \theta) = \text{Bern}(y; \theta)$$

Working under the sentiment analysis problem where we'd only like to flag words that appear (instead of counting), our class-conditional distribution will be Multinoulli:

$$p(\mathbf{x}|y; \pi_0, \pi_1) \propto \prod_{j=1}^m \pi_{yj}^{x_j}$$

We can then calculate our desired probabilities of a datapoint being in a certain class given its features using Bayes' Rule:

$$p(y|\mathbf{x}) \propto p(\mathbf{x}|y; \pi_0, \pi_1) p(y; \theta)$$

# Multinomial Naive Bayes Loss

In a statistical context, our loss function will be to maximize the likelihood of the data:

$$\max_{\theta; \mathbf{\pi}_0, \mathbf{\pi}_1} \sum_{i=1}^n \ln p(\mathbf{x}_i, y_i) = \max_{\mathbf{\pi}_0, \mathbf{\pi}_1} \sum_{i=1}^n \ln p(\mathbf{x}_i|y_i; \mathbf{\pi}_0, \mathbf{\pi}_1) + \max_\theta \sum_{i=1}^n \ln p(y_i; \theta)$$

After a *bunch* of algebra, we get to the conclusion that:

$$\hat{\theta} = \frac{\mathbf{1y}}{n}$$

$$\hat{\pi}_{0} = \frac{\sum_{i=1}^n (1-y_i)\mathbf{x}_i}{\sum_{i=1}^n \sum_{j=1}^m (1-y_i) x_{ij}}$$

$$\hat{\pi}_{1} = \frac{\sum_{i=1}^n y_i\mathbf{x}_i}{\sum_{i=1}^n \sum_{j=1}^m y_i x_{ij}}$$

Intuitively, $$\hat{\theta}$$ is the proportion of samples in class 1, and each $$\hat{\pi}$$ is the normalized cumulative count vector over the features. Thus, we have derived non-MLE methods of finding the parameters using priors.

# Discriminant Probabilistic View

In practice, we would like to have a function that, having learned the parameters, decides whether a sample belongs in class 0 or 1. To do this, we can define a discriminant function where a sample is in class 1 if the function returns a positive number and 0 if the number is negative:

$$\begin{align*}
h(\mathbf{x}) &= (\ln p(\mathbf{x}|y=1) + \ln p(y=1)) - (\ln p(\mathbf{x}|y=0) + \ln p(y=0)) \\
&= \left[\ln \prod_{j=1}^m \pi_{1j}^{x_j} - \ln \prod_{j=1}^m \pi_{0j}^{x_j}  \right] + [\ln \theta - \ln (1 - \theta)]\\
&= \sum_{j=1}^m x_j \ln \frac{\pi_{1j}}{\pi_{0j}} + \ln \frac{\theta}{1-\theta} = \mathbf{x}^\top \left(\ln \frac{\pi_1}{\pi_0} \right) + \ln \frac{\theta}{1-\theta}
\end{align*}$$

We've recovered the form of a linear regression! Indeed, the binary classification problem ultimately reduces to a linear regression. Since much of the machinery of the linear regression is computationally easier, people often work under the regression framework.

If we choose to do this, then under the probabilistic framework of the linear regression, we would be maximizing:

$$\arg_\mathbf{w}\max \prod_{i=1}^n p(y_i | \mathbf{x}_i, \mathbf{w})$$

Notice that here, we are parameterizing the class-conditional distribution. This is known as the *discriminative* model. Note that this is different from the generative model since we are not parameterizing the entire joint distribution. The benefits of doing so is that we are only learning weights to discriminate between classes, whereas in the generative model, we are learning weights about proportions and class-conditional frequencies. However, in the generative framework, we have a way of randomly generating *new data*, whereas in the discriminative model, we do not have this possibility since we did not learn the parameters required to generate data.

In the next post, we'll talk more about the discriminative model and its relationship to the logistic regression.]]></content><author><name>Michael Ge</name></author><category term="datasci" /><category term="machine learning" /><category term="probability" /><category term="classification" /><summary type="html"><![CDATA[In binary classification problems, we have an input feature vector and we’d like to classify it into one of two classes. We did this by minimizing reasonable loss functions based on activation functions. In this very long post, we’ll take a probabilistic approach to classification and detail the generative framework.]]></summary></entry><entry><title type="html">Classification and Perceptron</title><link href="https://www.geemichael.com/datasci/2018/01/09/linear-classification.html" rel="alternate" type="text/html" title="Classification and Perceptron" /><published>2018-01-09T00:00:00-08:00</published><updated>2018-01-09T00:00:00-08:00</updated><id>https://www.geemichael.com/datasci/2018/01/09/linear-classification</id><content type="html" xml:base="https://www.geemichael.com/datasci/2018/01/09/linear-classification.html"><![CDATA[We now leave the land of predicting real-numbered values to look at data classification. The discussion will conclude with one of the fundamental concepts behind classification, the Perceptron algorithm.

# Introduction to Classification

The classification problem involves specifying which category a data point belongs to. For instance, if you have a feature vector of an individual's political preferences, a classification problem might be to classify the individual as a Democrat or Republican. This is an example of a *binary classification* since there are only two classes, Democrat or Republican. 

We can express our output space as:

$$\mathcal{y} = \{-1, 1\}$$

Note that this is different from the regression problem where $\mathcal{y} = \mathbb{R}$.

What if we wanted to also consider Libertarians, the Green Party, and Independents as possible political parties for an individual? We can instead do multiclass classification for $K$ classes where $$\mathcal{y} = \{C_1, C_2, \ldots, C_K \}$$. Multiclass classification can be found in a variety of applications such as image recognition, disease diagnostics, and product recommendation. In this post, we will only cover the binary case.

# KNN for Classification

Before discussing the parameterized classification problem, we first discuss the KNN solution. For some input feature vector $\mathbf{x}$, we classify $\mathbf{x}$ by taking the majority of the $k$ nearest points:

$$
\hat{y} = \begin{cases}
1 & \sum_{i=1}^k y^{(i)} > 0\\
-1 & \text{otherwise}
\end{cases}
$$

Of course, KNN is still subject to the same problems it had in the linear regression case, namely overfitting and the need to keep all your data around at all times.

# Binary Linear Classification

Let's take the same modeling approach we used in linear regression and use it to classify. Define a hypothesis function $h$:

$$h(\mathbf{x}; \mathbf{w}) = \mathbf{w}^\top\mathbf{x}$$

We can use this line (hyperplane) to split the space into two subspaces. If $$h(\mathbf{x}; \mathbf{w}) > 0$$, then we predict $\hat{y} = 1$. Otherwise, we predict $$\hat{y} = -1$$.

# Determining the Decision Boundary

The set of points on this hyperplane is known as the *decision boundary*. These are defined as the $\mathbf{x}$ where $$\mathbf{w}^\top\mathbf{x} = 0$$. To determine the decision boundary, we would like to find its orientation and position. 

The orientation of this hyperplane is determined solely by $\mathbf{w}$ since $\mathbf{w}$ is orthogonal to the hyperplane. The proof is simple:

$$\mathcal{S} = \{\mathbf{x} \in \mathcal{X} | \mathbf{w}^\top\mathbf{x} + w_0 = 0 \}$$

Let $\mathbf{x}_1, \mathbf{x}_2$ be in $\mathcal{S}$. Let $\mathbf{s} = \mathbf{x}_1 - \mathbf{x}_2$.

$$\begin{align*}
\mathbf{w}^\top\mathbf{s} &= \mathbf{w}^\top(\mathbf{x}_1 - \mathbf{x}_2) \\
&= \mathbf{w}^\top\mathbf{x}_1 - \mathbf{w}^\top\mathbf{x}_2 \\
& = w_0 - w_0 = 0
\end{align*}$$

So $\mathbf{w}$ is orthogonal to the hyperplane.

To determine the decision boundary's location, we can use Lagrange multipliers to solve an optimization problem. We will try to find the closest point to the origin (the location by definition):

$$\arg_{\mathbf{x}}\min \mathbf{x}^\top\mathbf{x}$$

$$s.t. \mathbf{w}^\top \mathbf{x} + w_0 = 0$$

We then incorporate the constraint for the Lagrange multiplier:

$$L(\mathbf{x}, \lambda) = \mathbf{x}^\top\mathbf{x} + \lambda(\mathbf{w}^\top\mathbf{x} + w_0 - 0)$$

$$\frac{\partial L(\mathbf{x}, \lambda)}{\partial \mathbf{x}} = 2\mathbf{x} + \lambda \mathbf{w} := 0$$

$$\frac{\partial L(\mathbf{x}, \lambda)}{\partial \lambda} = \mathbf{w}^\top\mathbf{x} + w_0 := 0$$

Solving the system of equations:

$$\mathbf{w}^\top(-\lambda\mathbf{w} / 2) + w_0 = 0$$

$$\lambda = \frac{2w_0}{\mathbf{w}^\top\mathbf{w}}$$

Thus, the distance from the origin is a function of $$ \frac{2w_0}{\mathbf{w}^\top\mathbf{w}}$$.

# Linear Separability

We know how to construct a decision boundary with a given $\mathbf{w}$, but we haven't looked at the data yet. Oftentimes, data may not be *linearly separable*, meaning there may not be a hyperplane that separates the data into the two classes. This could be due to noise or an incorrect linear space.

One solution would be to use basis functions. Doing so introduces nonlinearity that may correctly divide the data.

# Training and Loss

Now that we have an idea of how to construct a classification model, we now need to train it. But in order to train a model, we need a loss function. One instinct might be to try using the same distance-based loss functions we've seen thus far. Take, for instance, least squares:

$$\mathcal{L}(\mathbf{w}) = \sum_{i=1}^n (y_i - h(\mathbf{x}_i; \mathbf{w}))^2, y_i \in \{-1, 1\}$$

But think about what this loss is doing. Here, we penalize estimations based on their distance to the true class, $$\pm 1$$. This means that points that are correctly classified will be penalized based on how far away from the true class they are. Thus, an outlier point can drastically shift the boundary, even if it's classified correctly.

Instead, we need to target the accuracy of a classification, and for that, we'll use the Perceptron algorithm.

# Perceptron Algorithm

The Perceptron algorithm is as follows:

1. Check if point is classified correctly.
2. If it is, move on.
3. Otherwise, adjust weights with gradient.

To run perceptron, we'll need an accuracy-based loss function with an informative derivative that tells us how to change our weights.

# 0/1 Activation Function

*Activation functions* are the answer to our need for accuracy-based loss functions. Activation functions map real numbers to a discrete set of values. The first activation function we'll look it is the 0/1 activation function:

$$f_{0, 1}(z) = \begin{cases}
1 & z > 0 \\
0 & \text{otherwise}
\end{cases}$$

Our loss function would then be the sum of occurrences where the prediction and true value differ:

$$\mathcal{L}(\mathbf{w}) = \sum_{i=1}^n f_{0/1}(-h(\mathbf{x_i}; \mathbf{w}) \cdot y_i)$$

There is a problem with the 0/1 activtion loss, but to understand it, we'll first talk about how the derivative-based update would work.

# Gradient Descent

The idea of gradient descent is that we wish to update our weights in a way that minimizes the loss. To do so, we first compute the gradient of our loss with respect to our weights, $$\frac{\partial \mathcal{L}(\mathbf{w})}{\partial \mathbf{w}}$$. This represents the slope vector of greatest ascent. We can then update our weights by subtracting this gradient vector scaled by some learning rate parameter, $\eta$:

$$\mathbf{w} \leftarrow \mathbf{w} - \eta \cdot \frac{\partial \mathcal{L}(\mathbf{w})}{\partial \mathbf{w}}$$

Unfortunately, the 0/1 activation function does not have an informative derivative. At $z=0$, the slope is undefined, and 0 otherwise. As a result, performing a weight adjustment based on the derivative will yield 0 change.

# ReLU Function

The solution to this would be to use the rectified linear unit activation function (ReLU). The function is defined as:

$$f_{relu}(z) = \begin{cases}
z & z > 0 \\
0 & \text{otherwise}
\end{cases} = \max(0, z)$$

In the 0/1 activation function, a misclassification always had an error value of 1. Here, the farther off a point is from its correct classification, the worse the penalty. As a result, the derivative is 1 for values greater than 0 and 0 otherwise, giving us a nonzero value during gradient descent!

# Perceptron Loss

Let's tie this all together. The Perceptron loss is:

$$\mathcal{L}(\mathbf{w}) = \sum_{i=1}^n f_{relu}(-h(\mathbf{x}_i; \mathbf{w}) \cdot y_i) = \sum_{i=1; y_i \neq \hat{y}_i}^n (\mathbf{w}^\top\mathbf{x}_i + w_0) \cdot y_i$$

In other words, this loss is the total distance of all the misclassified points.

# Stochastic Gradient Descent (SGD)

The update step requires taking the derivative of the loss with respect to your weights. This requires going through all your data over and over again and can often be time consuming. Another strategy commonly used in practice is stochastic gradient descent (SGD). In SGD, a gradient descent update is performed for every individual datapoint's loss until model convergence, rather than after observing all the data. Oftentimes, this performs better in practice than regular gradient descent.

To be clear, the gradient of the total-data loss would be:

$$\frac{\partial \mathcal{L}(\mathbf{w})}{\partial \mathbf{w}} = - \sum_{i=1; y_i \neq \hat{y}_i}^n y_i \cdot \mathbf{x}_i$$

Whereas the stochastic, single-point version would be:

$$\frac{\partial \mathcal{L}^{(i)}(\mathbf{w})}{\partial \mathbf{w}} = -y_i \mathbf{x}_i$$

So the update after each datapoint would be:

$$\mathbf{w}^{(t+1)} \leftarrow \mathbf{w}^{(t)} + \eta y_i \mathbf{x}_i$$

# The Perceptron Algorithm, Detailed

The final Perceptron algorithm is therefore:

1. Iterate over data
- If all classifications are correct, do nothing
- Else, add $y_i \mathbf{x}_i$ and $y_i$ to w_0 (don't forget to perform the update rule by taking the gradient with respect to $w_0$ as well, this wasn't detailed here)
2. Repeat until no classification errors are made

If the data is linearly separable, the algorithm will converge!]]></content><author><name>Michael Ge</name></author><category term="datasci" /><category term="machine learning" /><category term="classification" /><category term="perceptron" /><summary type="html"><![CDATA[We now leave the land of predicting real-numbered values to look at data classification. The discussion will conclude with one of the fundamental concepts behind classification, the Perceptron algorithm.]]></summary></entry><entry><title type="html">Linear Regression: Bayesian Approach, Normal Conjugacy</title><link href="https://www.geemichael.com/datasci/2018/01/08/bayesian-regression.html" rel="alternate" type="text/html" title="Linear Regression: Bayesian Approach, Normal Conjugacy" /><published>2018-01-08T00:00:00-08:00</published><updated>2018-01-08T00:00:00-08:00</updated><id>https://www.geemichael.com/datasci/2018/01/08/bayesian-regression</id><content type="html" xml:base="https://www.geemichael.com/datasci/2018/01/08/bayesian-regression.html"><![CDATA[Understanding the linear regression from a probabilistic perspective allows us to perform more advanced statistical inference. Today, we'll be applying Bayesian inference concepts to the linear regression. As a result, we'll have a way to update the beliefs of our models as more data becomes accessible or account for prior knowledge when looking at data.

I highly recommend taking a look at <a class="link" href="/datasci/2017/09/10/inference-coins/">this introductory post on inference</a> before delving into this post as it covers the fundamentals of MLE, MAP, and conjugacy.

Recall from the last post on <a class="link" href="{{page.previous.url}}">{{ page.previous.title }}</a> that we can avoid overfitting our models with regularization terms. Here is the $\ell_2$ norm loss, for example:

$$\min_{\mathbf{w}} \mathcal{L}(\mathbf{w}) + \lambda \mathbf{w}^\top\mathbf{w}$$

We use $\lambda$ to be an adjustable hyperparameter that specifies how much weight we put on the regularization term. Regularization terms can be effective, but they're rather *ad hoc* and inflexible.

# Bayesian Basics

Another solution to avoid overfitting to the data is to use Bayesian methods. In the Bayesian framework, we have a prior belief on our weights, $\mathbf{w}$, which we would like to use to maximize the likelihood of our data $D$, denoted $p(D\|\mathbf{w})$.

For a super brief overview of Bayes' Rule, we define the posterior, $p(\mathbf{w}\|D)$, as the product of the likelihood and prior:

$$p(\mathbf{w}|D) = \frac{\overbrace{p(D|\mathbf{w})}^\text{likelihood}\overbrace{p(\mathbf{w})}^\text{prior}}{p(D)} \propto p(D|\mathbf{w})p(\mathbf{w})$$

$$p(D) = \int_{\mathbf{w}} p(D|\mathbf{w})p(\mathbf{w})d\mathbf{w} = \text{constant for all }\mathbf{w}$$

# Choosing a Prior

We've discussed how to calculate the likelihood before. It's simply the product of independent probabilities of each data point. But how do we choose the prior? Often, the prior distribution is informed by a field expert or taken from a previous experiment. Otherwise, you can use a *flat prior*, one that provides little information in the prior.

# The form of the posterior

In the linear regression context, the likelihood of our data comes from a multivariate Normal distribution $\mathcal{N}(\mathbf{y}\|\mathbf{Xw}, \beta^{-1}\mathbf{I})$ with an assumed (fixed) precision $\beta$. Suppose we define our prior on $\mathbf{w}$ to be $\mathcal{N}(\mathbf{w}\|\mathbf{m}_0, \mathbf{S}_0)$. Our posterior is then proportional to:

$$p(\mu|D) \propto p(D|\mu)p(\mu)$$

$$=\mathcal{N}(\mathbf{y}|\mathbf{Xw}, \beta^{-1}\mathbf{I})\mathcal{N}(\mathbf{w}|\mathbf{m}_0, \mathbf{S_0})$$

After multiplying out the two distributions, completing the square, and doing a whole lot of algebra in between, we arrive at the conclusion that the posterior distribution is indeed multivariate Normal:

$$\mathbf{w} \sim \mathcal{N}(\mathbf{m}_n, \mathbf{S}_n)$$

$$\mathbf{S}_n = (\mathbf{S}_0^{-1} + \beta \mathbf{X}^\top\mathbf{X})^{-1}$$

$$\mathbf{m}_n = \mathbf{S}_n(\mathbf{S}_0^{-1}\mathbf{m}_0 + \beta \mathbf{X}^\top \mathbf{y})$$

This means that the Normal distribution is a conjugate of itself with the posterior form having the above parameters.

# MAP Estimator

The MAP estimator for $\mu$ is then $\mathbf{m}_n$ instead of $\mathbf{m}_0$. This is a vector containing the average values of each feature.]]></content><author><name>Michael Ge</name></author><category term="datasci" /><category term="machine learning" /><category term="bayesian" /><category term="regression" /><summary type="html"><![CDATA[Understanding the linear regression from a probabilistic perspective allows us to perform more advanced statistical inference. Today, we’ll be applying Bayesian inference concepts to the linear regression. As a result, we’ll have a way to update the beliefs of our models as more data becomes accessible or account for prior knowledge when looking at data.]]></summary></entry></feed>